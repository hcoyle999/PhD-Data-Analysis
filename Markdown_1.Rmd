---
title: "Markdown_1"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
```{r}
library (here)
location <- here("3.Analysis","Data","mydatafile.csv")
print(location)
```
```{r packageload, message=FALSE}
library(tidyverse)
library(skimr)
library (here)
```
```{r skimframes}
skim(frames)
```

```
loc <- here("3.Analysis","Data","frames_ex2.csv")
frames <- read_csv(file=loc)
```

```{r datadescription, message=FALSE}
frames %>%
group_by(test_item) %>%
summarise (
mean_resp = mean (response),
sd_resp= sd(response),
count = n ()
) %.%
ungroup ()
```

frames%>%

## R Markdown

